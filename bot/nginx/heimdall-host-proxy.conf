map $http_upgrade $connection_upgrade {
  default upgrade;
  '' close;
}

upstream heimdall_dashboard {
  server 127.0.0.1:3000;
}

upstream heimdall_api {
  server 127.0.0.1:3001;
}

upstream heimdall_ws {
  server 127.0.0.1:3002;
}

server {
  listen 80;
  server_name dashboard.example.com;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    proxy_pass http://heimdall_dashboard;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

server {
  listen 80;
  server_name api.example.com;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    proxy_pass http://heimdall_api;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

server {
  listen 80;
  server_name ws.example.com;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    proxy_pass http://heimdall_ws;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 600s;
  }
}

# After certificates are issued, replace the three HTTP server blocks above with HTTPS
# server blocks (or run `certbot --nginx` to let Certbot inject them automatically).
#
# Example HTTPS block:
# server {
#   listen 443 ssl http2;
#   server_name dashboard.example.com;
#
#   ssl_certificate /etc/letsencrypt/live/dashboard.example.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/dashboard.example.com/privkey.pem;
#
#   location / {
#     proxy_pass http://heimdall_dashboard;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
