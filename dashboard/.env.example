# Dashboard Environment Variables
NEXTAUTH_SECRET=your-secret-key-here

# For NextAuth v5, prefer AUTH_URL over NEXTAUTH_URL
# In production, set this to your actual domain (e.g., https://yourdomain.com)
AUTH_URL=http://localhost:3000
# Fallback for older NextAuth versions
NEXTAUTH_URL=http://localhost:3000

# For production behind reverse proxy, set this to true
AUTH_TRUST_HOST=true

# Discord OAuth, must be the same as the bot's client ID and secret
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Bot API Connection
BOT_API_URL=http://localhost:3001
INTERNAL_API_KEY=

# Optional: Database (if using database session storage)
# DATABASE_URL="postgresql://user:password@localhost:5432/heimdall"
